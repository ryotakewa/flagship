<div class="field">
  <dl>
    <dt>ID</dt>
    <dd><%= @category.id %></dd>
    <dt>カテゴリ</dt>
    <dd><%= @category.category.name if @category.category.present? %></dd>
    <dt>名称</dt>
    <dd><%= @category.name %></dd>
  </dl>
</div>

<div class="sp">
  <%= link_to '一覧', admin_categories_path %>
  <%= link_to '編集', edit_admin_category_path(@category) %>
</div>
